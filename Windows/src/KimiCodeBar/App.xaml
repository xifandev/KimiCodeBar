<Application
    x:Class="KimiCodeBar.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KimiCodeBar"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    xmlns:views="using:KimiCodeBar.Views.Controls">

    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <controls:XamlControlsResources />
            </ResourceDictionary.MergedDictionaries>

            <ResourceDictionary>

                <!-- ===== 主题色（文档第 8 节，Light / Dark 双主题） ===== -->
                <Color x:Key="KimiBlueColor">#FF3B82F5</Color>
                <SolidColorBrush x:Key="KimiBlueBrush" Color="{StaticResource KimiBlueColor}" />

                <ResourceDictionary.ThemeDictionaries>
                    <ResourceDictionary x:Key="Light">
                        <SolidColorBrush x:Key="PanelBackgroundBrush" Color="#F2F2F7" />
                        <SolidColorBrush x:Key="CardBackgroundBrush" Color="#FCFCFC" />
                        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#1F1F1F" />
                        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#595959" />
                        <SolidColorBrush x:Key="TextTertiaryBrush" Color="#808080" />
                        <SolidColorBrush x:Key="TrackBrush" Color="#1F1F1F" Opacity="0.10" />
                        <SolidColorBrush x:Key="HoverBrush" Color="#1F1F1F" Opacity="0.10" />
                    </ResourceDictionary>
                    <ResourceDictionary x:Key="Default">
                        <SolidColorBrush x:Key="PanelBackgroundBrush" Color="#0F1421" />
                        <SolidColorBrush x:Key="CardBackgroundBrush" Color="#1C2436" />
                        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#FFFFFF" />
                        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#8C8C8C" />
                        <SolidColorBrush x:Key="TextTertiaryBrush" Color="#666666" />
                        <SolidColorBrush x:Key="TrackBrush" Color="#FFFFFF" Opacity="0.10" />
                        <SolidColorBrush x:Key="HoverBrush" Color="#FFFFFF" Opacity="0.10" />
                    </ResourceDictionary>
                </ResourceDictionary.ThemeDictionaries>

                <!-- ===== 自定义控件默认样式 ===== -->
                <Style TargetType="views:ActionButton">
                    <Setter Property="Background" Value="{ThemeResource HoverBrush}" />
                    <Setter Property="Foreground" Value="{ThemeResource TextSecondaryBrush}" />
                    <Setter Property="BorderBrush" Value="Transparent" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="CornerRadius" Value="10" />
                    <Setter Property="HorizontalAlignment" Value="Stretch" />
                    <Setter Property="VerticalAlignment" Value="Stretch" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="views:ActionButton">
                                <Grid
                                    x:Name="RootGrid"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="{TemplateBinding CornerRadius}">
                                    <VisualStateManager.VisualStateGroups>
                                        <VisualStateGroup x:Name="CommonStates">
                                            <VisualState x:Name="Normal" />
                                            <VisualState x:Name="PointerOver">
                                                <Storyboard>
                                                    <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootGrid" Storyboard.TargetProperty="Background">
                                                        <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource HoverBrush}" />
                                                    </ObjectAnimationUsingKeyFrames>
                                                    <ObjectAnimationUsingKeyFrames Storyboard.TargetName="TitleText" Storyboard.TargetProperty="Foreground">
                                                        <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextPrimaryBrush}" />
                                                    </ObjectAnimationUsingKeyFrames>
                                                </Storyboard>
                                            </VisualState>
                                            <VisualState x:Name="Pressed">
                                                <Storyboard>
                                                    <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootGrid" Storyboard.TargetProperty="Background">
                                                        <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextPrimaryBrush}" Opacity="0.18" />
                                                    </ObjectAnimationUsingKeyFrames>
                                                </Storyboard>
                                            </VisualState>
                                            <VisualState x:Name="Disabled">
                                                <Storyboard>
                                                    <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootGrid" Storyboard.TargetProperty="Opacity">
                                                        <DiscreteObjectKeyFrame KeyTime="0" Value="0.4" />
                                                    </ObjectAnimationUsingKeyFrames>
                                                </Storyboard>
                                            </VisualState>
                                        </VisualStateGroup>
                                    </VisualStateManager.VisualStateGroups>

                                    <StackPanel
                                        Orientation="Vertical"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Spacing="6"
                                        Padding="0,12,0,12">
                                        <FontIcon
                                            x:Name="GlyphIcon"
                                            Glyph="{TemplateBinding Glyph}"
                                            FontSize="16"
                                            Visibility="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=GlyphVisibility}" />
                                        <TextBlock
                                            x:Name="TextIconBlock"
                                            Text="{TemplateBinding TextIcon}"
                                            FontSize="14"
                                            FontWeight="Bold"
                                            HorizontalAlignment="Center"
                                            Visibility="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=TextIconVisibility}" />
                                        <TextBlock
                                            x:Name="TitleText"
                                            Text="{TemplateBinding Title}"
                                            FontSize="11"
                                            FontWeight="Medium"
                                            HorizontalAlignment="Center"
                                            Foreground="{TemplateBinding Foreground}" />
                                    </StackPanel>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>

            </ResourceDictionary>
        </ResourceDictionary>
    </Application.Resources>

</Application>
